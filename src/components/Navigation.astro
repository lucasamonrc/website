---
import { PAGES } from "../consts";
import ThemeToggle from "./ThemeToggle.astro";

const currentPath = Astro.url.pathname;
---

<header
	class="mb-12 flex w-full items-center justify-between border-b border-border pb-4"
>
	<a
		class="font-serif text-xl font-light tracking-tight text-text no-underline"
		href="/"
	>
		Lucas Castro
	</a>
	<nav class="flex items-center gap-6">
		{
			PAGES.map((page) => {
				const isActive = currentPath.startsWith(page.to);
				return (
					<a
						href={page.to}
						class:list={[
							"relative text-sm no-underline transition-colors after:absolute after:bottom-0 after:left-0 after:h-px after:w-0 after:bg-current after:transition-[width] after:duration-150 hover:after:w-full",
							isActive
								? "font-medium text-text"
								: "text-text-secondary hover:text-accent",
						]}
					>
						{page.label}
					</a>
				);
			})
		}
		<ThemeToggle />
	</nav>
</header>
