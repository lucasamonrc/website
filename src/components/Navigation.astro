---
import { PAGES } from "../consts";
import ThemeToggle from "./ThemeToggle.astro";

const currentPath = Astro.url.pathname;
---

<header
	class="mb-12 flex w-full items-center justify-between border-b border-[var(--color-border)] pb-4 dark:border-[var(--color-border-dark)]"
>
	<a
		class="text-lg font-semibold tracking-tight no-underline"
		style="color: var(--color-text);"
		href="/"
	>
		Lucas Castro
	</a>
	<nav class="flex items-center gap-6">
		{
			PAGES.map((page) => {
				const isActive = currentPath.startsWith(page.to);
				return (
					<a
						href={page.to}
						class:list={[
							"text-sm no-underline transition-colors",
							isActive
								? "font-medium"
								: "hover:text-[var(--color-accent)] dark:hover:text-[var(--color-accent-dark)]",
						]}
						style={
							isActive
								? `color: var(--color-text);`
								: `color: var(--color-text-secondary);`
						}
					>
						{page.label}
					</a>
				);
			})
		}
		<ThemeToggle />
	</nav>
</header>
